<ui:FancyWindow
    xmlns="https://spacestation14.io"
    xmlns:ui="clr-namespace:Content.Client.UserInterface.Controls;assembly=Content.Client"
    Title="{Loc 'discord-window-title'}"
    MinSize="450 250"
    SetSize="450 250">
    <BoxContainer Orientation="Vertical" Margin="12">
        <PanelContainer StyleClasses="AngleRect">
            <BoxContainer Orientation="Vertical" Margin="12 10" HorizontalExpand="True">
                <Label Text="{Loc 'discord-window-header'}"
                       StyleClasses="LabelHeadingBigger"
                       HorizontalAlignment="Center" />
                <Control MinSize="0 8" />
                <RichTextLabel Text="{Loc 'discord-window-description'}"
                               HorizontalExpand="True" />
            </BoxContainer>
        </PanelContainer>
        <Control MinSize="0 10" />
        <ui:StripeBack>
            <BoxContainer Orientation="Horizontal" HorizontalAlignment="Center" Margin="8 6">
                <Button Name="PatreonButton"
                        Access="Public"
                        Text="{Loc 'discord-window-button'}"
                        StyleClasses="ButtonBig"
                        MinWidth="220"
                        MinHeight="40" />
            </BoxContainer>
        </ui:StripeBack>
    </BoxContainer>
</ui:FancyWindow>
