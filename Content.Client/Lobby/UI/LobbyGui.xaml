<!--
SPDX-FileCopyrightText: 2021 Vera Aguilera Puerto <6766154+Zumorica@users.noreply.github.com>
SPDX-License-Identifier: AGPL-3.0-or-later
-->

<lobbyUi:LobbyGui
    xmlns="https://spacestation14.io"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:maths="clr-namespace:Robust.Shared.Maths;assembly=Robust.Shared.Maths"
    xmlns:cc="clr-namespace:Content.Client.Administration.UI.CustomControls"
    xmlns:controls="clr-namespace:Content.Client.UserInterface.Controls"
    xmlns:vote="clr-namespace:Content.Client.Voting.UI"
    xmlns:style="clr-namespace:Content.Client.Stylesheets"
    xmlns:lobbyUi="clr-namespace:Content.Client.Lobby.UI"
    xmlns:info="clr-namespace:Content.Client.Info"
    xmlns:widgets="clr-namespace:Content.Client.UserInterface.Systems.Chat.Widgets">

    <!-- Background -->
    <TextureRect Access="Public" VerticalExpand="True" HorizontalExpand="True" Name="Background"
                 Stretch="KeepAspectCovered" />

    <BoxContainer Name="MainContainer" VerticalExpand="True" HorizontalExpand="True"
                  Orientation="Horizontal" Margin="10 10 10 10" SeparationOverride="2">
        <SplitContainer State="Auto" ResizeMode="NotResizable" HorizontalExpand="True">

            <!-- LEFT: только ссылки сверху и инфо снизу, арт виден -->
            <BoxContainer Name="LeftSide" Orientation="Vertical" SeparationOverride="4" HorizontalExpand="True">
                <Control Name="DefaultState" VerticalExpand="True">

                    <!-- Ссылки сверху слева, компактно -->
                    <BoxContainer Name="TopLeft" Orientation="Vertical">
                        <PanelContainer StyleClasses="AngleRect" HorizontalAlignment="Left"
                                        Name="LeftSideTop" VerticalAlignment="Top">
                            <BoxContainer Orientation="Vertical" HorizontalAlignment="Left">
                                <info:LinkBanner Name="LinkBanner" VerticalExpand="false"
                                                 HorizontalAlignment="Left" Margin="3 3 3 3" />
                            </BoxContainer>
                        </PanelContainer>

                        <!-- Голосования -->
                        <BoxContainer Orientation="Vertical" SeparationOverride="4" Name="VoteContainer"
                                      Access="Public" HorizontalAlignment="Left" VerticalAlignment="Top"
                                      Margin="0 4" />
                    </BoxContainer>

                    <Control VerticalExpand="True" />

                    <!-- Снизу слева: девинфо + песня + фон -->
                    <BoxContainer Orientation="Horizontal" HorizontalAlignment="Left"
                                  VerticalAlignment="Bottom" SeparationOverride="4">
                        <info:DevInfoBanner Name="DevInfoBanner" VerticalExpand="false" Margin="3 3 3 3" />
                        <PanelContainer StyleClasses="AngleRect">
                            <RichTextLabel Name="LobbySong" Access="Public" HorizontalAlignment="Center" />
                        </PanelContainer>
                        <PanelContainer StyleClasses="AngleRect">
                            <RichTextLabel Name="LobbyBackground" Access="Public" HorizontalAlignment="Center" />
                        </PanelContainer>
                    </BoxContainer>
                </Control>

                <Control Access="Public" Visible="False" Name="CharacterSetupState" VerticalExpand="True" />
            </BoxContainer>

            <!-- RIGHT: вся инфо справа -->
            <PanelContainer Name="RightSide" Access="Public" StyleClasses="AngleRect"
                            HorizontalAlignment="Right" VerticalExpand="True" VerticalAlignment="Stretch">
                <BoxContainer Orientation="Vertical" HorizontalExpand="True">

                    <!-- Название сервера -->
                    <BoxContainer Orientation="Horizontal" MinSize="0 36" Name="HeaderContainer"
                                  Access="Public" SeparationOverride="4">
                        <Label Name="ServerName" Access="Public" StyleClasses="LabelHeadingBigger"
                               VAlign="Center" HorizontalExpand="True" HorizontalAlignment="Center" />
                    </BoxContainer>

                    <controls:HLine Color="{x:Static style:StyleNano.NanoGold}" Thickness="2" />
                    <controls:HSpacer Spacing="6" />

                    <!-- Готов / Наблюдать сразу под названием -->
                    <BoxContainer Orientation="Vertical" SeparationOverride="4" Margin="4 0 4 0">
                        <BoxContainer Orientation="Horizontal" SeparationOverride="4">
                            <Button Name="ReadyButton" Access="Public" ToggleMode="True"
                                    Text="{Loc 'ui-lobby-ready-up-button'}"
                                    StyleClasses="ButtonBig" HorizontalExpand="True" />
                            <cc:UICommandButton Command="observe" Name="ObserveButton" Access="Public"
                                                Text="{Loc 'ui-lobby-observe-button'}"
                                                StyleClasses="ButtonBig" HorizontalExpand="True"
                                                WindowType="{x:Type lobbyUi:ObserveWarningWindow}" />
                        </BoxContainer>
                        <Label Name="StartTime" Access="Public" Align="Center"
                               FontColorOverride="{x:Static maths:Color.LightGray}"
                               StyleClasses="LabelBig" HorizontalAlignment="Center" />
                        <RichTextLabel Name="PlaytimeComment" Visible="False" Access="Public"
                                       HorizontalAlignment="Center" />
                    </BoxContainer>

                    <controls:HSpacer Spacing="6" />
                    <controls:HLine Color="{x:Static style:StyleNano.NanoGold}" Thickness="1" />
                    <controls:HSpacer Spacing="6" />

                    <!-- Кнопки меню -->
                    <BoxContainer Orientation="Horizontal" HorizontalAlignment="Right" SeparationOverride="4">
                        <Button Name="AHelpButton" Access="Public"
                                Text="{Loc 'ui-lobby-ahelp-button'}" StyleClasses="ButtonBig" />
                        <vote:VoteCallMenuButton Name="CallVoteButton" StyleClasses="ButtonBig" />
                        <Button Name="OptionsButton" Access="Public" StyleClasses="ButtonBig"
                                Text="{Loc 'ui-lobby-options-button'}" />
                        <Button Name="LeaveButton" Access="Public" StyleClasses="ButtonBig"
                                Text="{Loc 'ui-lobby-leave-button'}" />
                    </BoxContainer>

                    <controls:HSpacer Spacing="6" />
                    <controls:HLine Color="{x:Static style:StyleNano.NanoGold}" Thickness="1" />
                    <controls:HSpacer Spacing="6" />

                    <!-- Серверная инфо -->
                    <controls:NanoHeading Text="{Loc 'ui-lobby-server-info-block'}" />
                    <info:ServerInfo Name="ServerInfo" Access="Public" MinSize="0 30"
                                     VerticalExpand="false" Margin="3 3 3 3"
                                     MaxWidth="400" HorizontalAlignment="Left" />
                    <Label Name="StationTime" Access="Public"
                           FontColorOverride="{x:Static maths:Color.LightGray}"
                           Margin="3 3 3 3" HorizontalAlignment="Left" />

                    <controls:HSpacer Spacing="4" />

                    <!-- Персонаж -->
                    <lobbyUi:LobbyCharacterPreviewPanel Name="CharacterPreview" Access="Public" />

                    <controls:HSpacer Spacing="4" />

                    <Label Name="Balance" Access="Public" StyleClasses="LabelHeadingBigger"
                           VAlign="Center" HorizontalExpand="True" HorizontalAlignment="Center" />

                    <controls:HLine Color="{x:Static style:StyleNano.NanoGold}" Thickness="2" Access="Public" />
                    <controls:HSpacer Spacing="6" />

                    <!-- Чат -->
                    <widgets:ChatBox Name="Chat" Access="Public" VerticalExpand="True"
                                     Margin="3 3 3 3" MinHeight="50" />
                </BoxContainer>

                <TextureButton Name="CollapseButton"
                               TexturePath="filled_right_arrow.svg.192dpi"
                               HorizontalAlignment="Right"
                               VerticalAlignment="Top"
                               Margin="0 2 2 0"
                               ModulateSelfOverride="#DEDEDE"
                               Scale="0.5 0.5" />
            </PanelContainer>
        </SplitContainer>

        <PanelContainer Name="ExpandPanel"
                        StyleClasses="AngleRect"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Top"
                        Margin="0 2 2 0"
                        Visible="False">
            <TextureButton Name="ExpandButton"
                           TexturePath="filled_left_arrow.svg.192dpi"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           ModulateSelfOverride="#DEDEDE"
                           Scale="0.5 0.5" />
        </PanelContainer>
    </BoxContainer>
</lobbyUi:LobbyGui>
